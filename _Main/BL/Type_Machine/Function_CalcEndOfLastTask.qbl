Quintiq file version 2.0
#parent: #root
Function CalcEndOfLastTask
{
  TextBody:
  [*
    // mho23702 Feb-20-2020 (created)
    
    value := guard( this.LastTask().End(), this.Company().Time() );
    
    this.EndOfLastTask( value );
  *]
}
