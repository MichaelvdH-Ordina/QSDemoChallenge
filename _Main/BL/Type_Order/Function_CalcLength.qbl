Quintiq file version 2.0
#parent: #root
Function CalcLength
{
  TextBody:
  [*
    value := 0;
    if( not isnull( this.Article() ) )
    {
      value := this.Article().Length();
    }
    
    this.Length( value );
  *]
}
