Quintiq file version 2.0
#parent: #root
Function CalcWidth
{
  TextBody:
  [*
    value := 0.0;
    if( not isnull( this.Article() ) )
    {
      value := this.Article().Width();
    }
    
    this.Width( value );
  *]
}
