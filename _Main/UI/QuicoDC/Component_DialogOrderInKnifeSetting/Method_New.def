Quintiq file version 2.0
#parent: #root
Method New (GUIComponent parent, Order owner) as OrderInKnifeSetting id:Method_DialogOrderInKnifeSetting_New
{
  #keys: '[409348.0.114287222]'
  Body:
  [*
    data := owner.OrderInKnifeSetting( relshadow );
    
    Dialog.Data( data );
    
    if ( Dialog.DoModal( parent ) > 0 )
    {
      data.Commit();
    }
    
    return data.WrappedInstance();
  *]
}
